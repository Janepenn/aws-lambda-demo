{
  "name": "aws-lambda-demo",
  "version": "1.0.0",
  "description": "An aws lambda demo",
  "main": "index.js",
  "scripts": {
    "start": "sls offline -s dev",
    "deploy": "sls deploy -s dev",
    "remove": "sls remove -s dev",
    "deploy:prod": "NODE_ENV=prod sls deploy",
    "remove:prod": "NODE_ENV=prod sls remove"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless-dotenv-plugin": "^3.12.2",
    "serverless-offline": "^8.5.0"
  },
  "dependencies": {
    "aws-sdk": "^2.1104.0",
    "jsonwebtoken": "^8.5.1"
  }
}
